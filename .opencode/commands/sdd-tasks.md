---
description: Fase 4 del flujo SDD — descompón el plan en tareas verificables
agent: sdd-tasks
---

Fase 4 del flujo SDD: descomposición en tareas.

Spec: $ARGUMENTS (número `NNN`, slug o ruta)

Lee `spec.md` y `plan.md` de esa spec y escribe `tasks.md`: fases en orden de dependencia, tareas
de 20–30 min, cada una con `RF:` y `Hecho cuando:` objetiva, lógica pura con sus unit tests.
