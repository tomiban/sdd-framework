---
description: Fase 3 del flujo SDD — escribe el plan técnico de una spec
agent: sdd-plan
---

Fase 3 del flujo SDD: plan sin código.

Spec: $ARGUMENTS (número `NNN`, slug o ruta). Si hay hallazgos QA de la conversación, pégalos en
este mensaje; si no, incluye la sección de resoluciones vacía para retomarla después.

Escribe `plan.md` con persistencia derivada (sección 0), resoluciones A#, decisiones D#, modelo de
datos en JSON, estrategia de tests y cobertura RF completa.