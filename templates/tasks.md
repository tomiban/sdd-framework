# Tasks — Spec NNN <título de la funcionalidad>

> Orden de dependencia obligatorio. Cada tarea deja `pnpm test`, `pnpm run typecheck` y
> `pnpm run build` en verde.

## T1 — <nombre de la tarea>

**RF:** RF-x · CL-y

- <acción verificable>

**Hecho cuando:**
- [ ] <condición objetiva>
- [ ] `pnpm test`, `pnpm run typecheck` y `pnpm run build` en verde
