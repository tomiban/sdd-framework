---
description: Crea una spec de funcionalidad (fase 1 del SDD, vía agente sdd-spec)
agent: sdd-spec
---

Crea la spec de la funcionalidad: $ARGUMENTS

Delega en tu procedimiento de rol: lista `specs/`, calcula el siguiente número `NNN`, entrevista al
usuario si hace falta y redacta únicamente `specs/NNN-<nombre>/spec.md` con las secciones del flujo
SDD y RF en EARS. No toques código ni `docs/`.